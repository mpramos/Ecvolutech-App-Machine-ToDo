import styled from 'styled-components'

const Form = styled.form`
        background-color: red;
        display: flex;
        flex-direction: column;
        gap:10px;
        padding:20px;

`
export default Form

